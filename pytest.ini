[pytest]
testpaths = tests
# The scripts under custom_components/.../tests are manual diagnostics that need
# a Home Assistant install; the suite in tests/ runs hardware-free.
norecursedirs = custom_components .git .venv
filterwarnings =
    error::RuntimeWarning
